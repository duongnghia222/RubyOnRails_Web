

<h1>Friends</h1>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr class="table-dark">
      <th>Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Twitter</th>
      <th> </th>
    </tr>
  </thead>
  <tbody>
    <% @friends.each do |friend| %>
      
      <tr>
        <td>
          <%= friend.first_name + ' ' + friend.last_name%>
        </td>
        <td> <%= friend.email %></td>
        <td> <%= friend.phone %></td>
        <td> <%= friend.twitter %></td>
        <td> 
          <%= link_to 'Show', friend, class:"btn btn-primary btn-sm" %>
          <%= link_to 'Edit', friend , class:"btn btn-primary btn-sm" %>
          <%= link_to 'Delete', @friend, method: :delete, class:"btn btn-primary btn-sm" %>

        </td>

      </tr>
      
    <% end %>
  </tbody>
</table>


<!-- <div id="friends">
  <% @friends.each do |friend| %>
    <%= render friend %>
    <p>
      <%= link_to "Show this friend", friend %>
    </p>
  <% end %>
</div> -->

<%= link_to "New friend", new_friend_path, class:'btn btn-secondary'%>
